services:
  postgres:
    image: postgres:16-alpine
    container_name: validika-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: validika
      POSTGRES_USER: jairo404
      POSTGRES_PASSWORD: UVQUG777
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: